info:
  name: Create a new user
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/createUser"
  body:
    type: json
    data: |-
      {
        "emailAddress": "jos.bosmans@example.com",
        "password": "hunter2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

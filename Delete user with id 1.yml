info:
  name: Delete user with id 1
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/deleteUse"
  body:
    type: json
    data: |-
      {
        "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

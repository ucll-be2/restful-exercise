info:
  name: Get all users older than 18 years
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/getAllUsers/minAge/18"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

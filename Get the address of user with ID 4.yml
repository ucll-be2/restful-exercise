info:
  name: Get the address of user with ID 4
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/addresses?userId=4"
  params:
    - name: userId
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

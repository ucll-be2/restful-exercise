info:
  name: Update user 12's emailAddress
  type: http
  seq: 7

http:
  method: POST
  url: "{{host}}/users/12"
  body:
    type: json
    data: |-
      {
        "emailAddress": "clement.peerens@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
